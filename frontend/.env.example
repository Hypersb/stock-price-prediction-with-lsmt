# Public browser configuration only.
# Never put DATABASE_URL, MARKET_DATA_API_KEY, or other secrets here.
#
# CLIENT-EXPOSED (bundled into the browser):
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# SERVER-ONLY (set in the Next.js server/container environment, not NEXT_PUBLIC_*):
# API_INTERNAL_BASE_URL=http://backend:8000
